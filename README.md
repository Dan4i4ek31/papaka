# 🛒 Papaka - E-Commerce API

Full-stack E-Commerce API на основе **FastAPI** с системой маркетплейса, функционалом авторов и листингов продуктов.

---

## 📋 Описание проекта

**Papaka** - это веб-приложение для интернет-магазина с современным REST API и фронтенд-интерфейсом. Проект реализует полную архитектуру e-commerce с поддержкой:

✅ Управление пользователями и ролями  
✅ Каталог товаров и листинги авторов  
✅ Корзина покупок и заказы  
✅ Система рейтингов и отзывов  
✅ Чат между пользователями  
✅ Админ-панель для управления контентом  
✅ JWT-аутентификация и авторизация  

---

## 🛠 Технологический стек

| Компонент | Технология |
|-----------|-----------|
| **Backend Framework** | FastAPI |
| **ORM** | SQLAlchemy 2.0 |
| **Database** | SQLite (dev) / PostgreSQL (prod) |
| **Authentication** | JWT (PyJWT) |
| **Password Hashing** | Bcrypt |
| **Migrations** | Alembic |
| **Templates** | Jinja2 |
| **Server** | Uvicorn |
| **Validation** | Pydantic |

---

## 📁 Структура проекта

```
papaka/
├── app/
│   ├── database/              # Конфигурация и подключение БД
│   │   ├── database.py        # Engine, SessionLocal, Base
│   │   └── ...
│   │
│   ├── models/                # SQLAlchemy ORM модели
│   │   ├── user.py            # Модель пользователя
│   │   ├── product.py         # Модель товара
│   │   ├── order.py           # Модель заказа
│   │   ├── review.py          # Модель отзыва
│   │   └── ...
│   │
│   ├── schemas/               # Pydantic схемы (валидация)
│   │   ├── user_schema.py
│   │   ├── product_schema.py
│   │   └── ...
│   │
│   ├── router/                # API маршруты (endpoints)
│   │   ├── user_router.py     # /api/users/*
│   │   ├── product_router.py  # /api/products/*
│   │   ├── order_router.py    # /api/orders/*
│   │   ├── cart_router.py     # /api/cart/*
│   │   ├── review_router.py   # /api/reviews/*
│   │   ├── admin_router.py    # /api/admin/*
│   │   └── ...
│   │
│   ├── services/              # Бизнес-логика
│   │   ├── user_service.py
│   │   ├── product_service.py
│   │   └── ...
│   │
│   ├── repositories/          # Слой доступа к данным (DAO)
│   │   ├── user_repository.py
│   │   ├── product_repository.py
│   │   └── ...
│   │
│   ├── exceptions/            # Кастомные исключения
│   │   └── handler.py         # Глобальная обработка ошибок
│   │
│   ├── utils/                 # Вспомогательные утилиты
│   │   └── ...
│   │
│   ├── templates/             # HTML шаблоны (Jinja2)
│   │   ├── index.html
│   │   ├── auth.html
│   │   ├── cart.html
│   │   ├── account.html
│   │   ├── chat.html
│   │   ├── favorite.html
│   │   └── admin.html
│   │
│   ├── static/                # Статические файлы
│   │   ├── css/
│   │   ├── js/
│   │   └── images/
│   │
│   ├── config.py              # Конфигурация приложения
│   └── dependencies.py        # FastAPI зависимости
│
├── migrations/                # Alembic миграции БД
│   ├── versions/
│   └── env.py
│
├── tests/                     # Тесты
│
├── main.py                    # Точка входа в приложение ⭐
├── requirements.txt           # Python зависимости
├── alembic.ini                # Конфигурация Alembic
├── pyproject.toml             # Метаданные проекта
├── .env                       # Переменные окружения (НЕ в git)
└── test.db                    # SQLite база данных
```

---

## 🚀 Быстрый старт

### Шаг 1️⃣: Клонирование репозитория

```bash
git clone https://github.com/Dan4i4ek31/papaka.git
cd papaka
```

### Шаг 2️⃣: Создание виртуального окружения

**На Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**На Windows (PowerShell):**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**На Windows (CMD):**
```cmd
python -m venv venv
venv\Scripts\activate.bat
```

### Шаг 3️⃣: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4️⃣: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Копируем пример (если существует)
cp .env.example .env

# Или создаём вручную
cat > .env << EOF
# Database Configuration
DATABASE_URL=sqlite:///./test.db

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Environment
ENVIRONMENT=development
EOF
```

⚠️ **Важно:** Измените `SECRET_KEY` на случайную строку из минимум 32 символов в продакшене!

```bash
# Генерируем надёжный SECRET_KEY
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Шаг 5️⃣: Инициализация базы данных

```bash
# Применяем миграции Alembic
alembic upgrade head
```

Или если таблицы не существуют, они создадутся автоматически при первом запуске приложения.

### Шаг 6️⃣: Запуск приложения

**Вариант 1 - Простой запуск (для разработки):**
```bash
python main.py
```

**Вариант 2 - С автоперезагрузкой (рекомендуется для разработки):**
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Вариант 3 - Production запуск:**
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

### ✅ Проверка, что всё работает

Откройте в браузере: **http://localhost:8000**

Вы должны увидеть главную страницу приложения.

---

## 📚 Документация API

После запуска приложения документация доступна по адресам:

| URL | Описание |
|-----|---------|
| **http://localhost:8000/docs** | 🟢 Swagger UI (интерактивная) |
| **http://localhost:8000/redoc** | 📘 ReDoc (статичная, красивая) |
| **http://localhost:8000/health** | ❤️ Health check |

### Быстрые примеры API запросов

**Создание пользователя (регистрация):**
```bash
curl -X POST "http://localhost:8000/api/users/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "password": "securepass123"
  }'
```

**Получение списка товаров:**
```bash
curl "http://localhost:8000/api/products/"
```

**Получение информации о здоровье приложения:**
```bash
curl "http://localhost:8000/health"
```

---

## 🌐 Веб-интерфейс

Приложение включает следующие HTML-страницы:

| Маршрут | Страница | Описание |
|---------|----------|---------|
| `/` | **index.html** | 🏠 Главная страница |
| `/auth.html` | **auth.html** | 🔐 Авторизация и регистрация |
| `/account.html` | **account.html** | 👤 Личный кабинет |
| `/cart.html` | **cart.html** | 🛒 Корзина покупок |
| `/favorite.html** | **favorite.html** | ❤️ Избранное (wishlist) |
| `/chat.html` | **chat.html** | 💬 Чат между пользователями |
| `/admin.html** | **admin.html** | ⚙️ Админ-панель |

---

## 🔐 Аутентификация

Проект использует **JWT (JSON Web Token)** для аутентификации.

### Процесс регистрации и входа

```
1. User POST /api/users/register
   ↓
2. Пароль хэшируется bcrypt
   ↓
3. Пользователь создаётся в БД
   ↓
4. User POST /api/auth/login
   ↓
5. Генерируется JWT токен
   ↓
6. Клиент отправляет токен в заголовке Authorization
   ↓
7. API проверяет токен для каждого защищённого маршрута
```

### Использование токена

```bash
# Добавьте токен в заголовок Authorization
curl -X GET "http://localhost:8000/api/users/me" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN_HERE"
```

---

## 💾 Работа с базой данных

### Просмотр SQLite БД

```bash
# Установка sqlite3 CLI (если не установлена)
# На macOS: brew install sqlite
# На Linux: sudo apt-get install sqlite3
# На Windows: скачайте с https://www.sqlite.org/download.html

# Открытие БД
sqlite3 test.db

# Полезные команды:
.tables              # Список таблиц
.schema users        # Структура таблицы
SELECT * FROM users LIMIT 5;  # Просмотр данных
.quit                # Выход
```

### Создание новой миграции

Когда вы добавляете новую модель в `app/models/`:

```bash
# Алембик автоматически сгенерирует изменения
alembic revision --autogenerate -m "Add new_table model"

# Просмотр созданной миграции
cat migrations/versions/xxxxx_add_new_table_model.py

# Применение миграции
alembic upgrade head

# Откат последней миграции (если нужно)
alembic downgrade -1
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# С подробным выводом
pytest -v

# Только определённый файл
pytest tests/test_users.py

# С покрытием кода
pytest --cov=app
```

---

## 🐛 Решение проблем

### Проблема 1: "ModuleNotFoundError: No module named 'fastapi'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/macOS

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема 2: "Port 8000 is already in use"

**Решение:**
```bash
# Запустите на другом порту
uvicorn main:app --port 8001

# Или найдите процесс, занимающий порт 8000
# Linux/macOS:
lsof -i :8000

# Windows:
netstat -ano | findstr :8000
```

### Проблема 3: Ошибки при импорте SQLAlchemy

**Решение:**
```bash
# Переустановите SQLAlchemy и зависимости БД
pip install --force-reinstall sqlalchemy alembic
```

### Проблема 4: БД не создаётся

**Решение:**
```bash
# Вручную создайте таблицы
python -c "from app.database.database import create_tables; create_tables()"

# Или удалите старую БД и начните заново
rm test.db
python main.py
```

---

## 📊 Основной функционал API

### 👥 Управление пользователями
- `POST /api/users/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/users/me` - Получить текущего пользователя
- `PUT /api/users/{user_id}` - Обновить профиль
- `DELETE /api/users/{user_id}` - Удалить аккаунт

### 🛍 Управление товарами
- `GET /api/products/` - Список всех товаров
- `GET /api/products/{product_id}` - Деталь товара
- `POST /api/products/` - Создать товар (админ)
- `PUT /api/products/{product_id}` - Обновить товар (админ)
- `DELETE /api/products/{product_id}` - Удалить товар (админ)

### 🛒 Корзина
- `GET /api/cart/` - Содержимое корзины
- `POST /api/cart/items` - Добавить товар
- `DELETE /api/cart/items/{item_id}` - Удалить товар

### 📦 Заказы
- `POST /api/orders/` - Создать заказ
- `GET /api/orders/` - Список заказов пользователя
- `GET /api/orders/{order_id}` - Деталь заказа

### ⭐ Отзывы
- `GET /api/reviews/` - Все отзывы
- `POST /api/reviews/` - Оставить отзыв
- `GET /api/reviews/product/{product_id}` - Отзывы товара

### 💬 Чат
- `GET /api/messages/` - История чатов
- `POST /api/messages/` - Отправить сообщение

### ⚙️ Админ-панель
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/stats` - Статистика
- `DELETE /api/admin/users/{user_id}` - Удалить пользователя

---

## 🔧 Расширенная конфигурация

### Использование PostgreSQL вместо SQLite

1. **Установите драйвер PostgreSQL:**
```bash
pip install psycopg2-binary
```

2. **Обновите `.env`:**
```env
DATABASE_URL=postgresql://user:password@localhost:5432/papaka
```

3. **Примените миграции:**
```bash
alembic upgrade head
```

### Использование переменных окружения

```python
# В app/config.py или app/dependencies.py
import os
from dotenv import load_dotenv

load_dotenv()

DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./test.db")
SECRET_KEY = os.getenv("SECRET_KEY", "change-me")
ALGORITHM = os.getenv("ALGORITHM", "HS256")
ACCESS_TOKEN_EXPIRE_MINUTES = int(os.getenv("ACCESS_TOKEN_EXPIRE_MINUTES", "30"))
```

---

## 📝 Полезные команды

```bash
# Виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
deactivate                 # Выход из venv

# Зависимости
pip list                   # Список установленных пакетов
pip freeze > requirements.txt  # Экспорт зависимостей

# Запуск приложения
python main.py
uvicorn main:app --reload

# Миграции БД
alembic init migrations
alembic revision --autogenerate -m "message"
alembic upgrade head
alembic downgrade -1

# Тестирование
pytest
pytest -v
pytest --cov=app

# Линтинг и форматирование (если установлены)
flake8 app/
black app/
isort app/
```

---

## 🤝 Участие в разработке

Если вы хотите улучшить проект:

1. Создайте fork репозитория
2. Создайте новую ветку (`git checkout -b feature/amazing-feature`)
3. Сделайте коммит изменений (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Проект распространяется без указанной лицензии. Для коммерческого использования свяжитесь с автором.

---

## 👤 Автор

**Dan4i4ek31** - Full-Stack разработчик

- GitHub: [@Dan4i4ek31](https://github.com/Dan4i4ek31)
- Репозиторий: [Dan4i4ek31/papaka](https://github.com/Dan4i4ek31/papaka)

---

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте раздел "🐛 Решение проблем"
2. Посмотрите в консоли логи ошибок
3. Откройте [Issue](https://github.com/Dan4i4ek31/papaka/issues) на GitHub

---

## 🎉 Спасибо за использование Papaka!

**Последнее обновление:** Декабрь 2025  
**Версия:** 1.0.0
